# Antigravity 프론트엔드 병렬작업 지시문 V2

> 상태: `STATIC_CANDIDATE` — 구현 및 브라우저 검증 전까지 완료로 간주하지 않는다.

이 문서는 다음 두 문서를 통합·수정한 최신 단일 기준본이다.

- `ANTIGRAVITY_BENEFITS_PARALLEL_PROMPT.md`
- `ANTIGRAVITY_COMMUNITY_PRIORITY_ADDENDUM.md`

충돌하는 지시가 있으면 이 V2 문서를 우선한다.

## 용어 정정

이전 계획의 관련 표현은 모두 `당근(밴드)`로 정정한다.

`당근(밴드)`는 특정 서비스의 데이터나 화면을 복제한다는 뜻이 아니다. `당근(밴드)`의 지역 기반성과 친목모임 성격을 결합한 참고 개념이며, 실제 사용자 화면에는 타사 브랜드를 노출하지 않고 `동네 모임` 또는 `지역 친목모임`으로 표현한다.

아래 코드 블록 전체를 Antigravity의 현재 프론트엔드 작업에 전달한다.

```text
1. TASK — 작업 목표

현재 Week 8-9 카카오맵 프로젝트의 프론트엔드를 담당하세요.

공공서비스 혜택 기능을 유지하면서, 사용 빈도가 높은 지역 생활·커뮤니티 추천을 제품의 기본 경험으로 강화합니다.

핵심 기능 우선순위:

1) 벙 맛집
2) 점심 뭐 먹지
3) 저녁 뭐 먹지
4) 지역 데이트 코스
5) 지역 헬스장
6) 산책로·카페·맛집·병원·약국·마트·일반 검색
7) 정부 혜택 탐색

여기서 `벙 맛집`은 지역 친목모임이 인원·예산·시간·분위기에 맞는 식사 장소 후보를 고르는 기능입니다.


2. EXPECTED OUTCOME — 결과물과 성공 기준

수정 허용 파일은 `frontend/public/index.html` 하나입니다.

완료 시 다음이 모두 가능해야 합니다.

- 첫 화면은 `내 주변 찾기`이며 기존 지도와 생활 검색이 즉시 보인다.
- `벙 맛집`이 가장 눈에 잘 띄는 동네 추천 행동으로 제공된다.
- 점심·저녁·데이트·헬스장·산책로·카페·맛집을 한 번에 탐색할 수 있다.
- 사용자가 직접 검색어를 입력하는 기존 자유 검색도 그대로 사용할 수 있다.
- 기존 공공 카페 1km·3km·5km 반경 검색이 유지된다.
- 정부 혜택 탭과 데이터 연결 상태 패널이 유지된다.
- 현재 데이터가 보장하지 않는 평점·영업·예약·단체석 정보를 사실처럼 표시하지 않는다.


3. REQUIRED SKILLS — 적용할 역량

- frontend-design: 기존 디자인 언어를 유지한 증분 UI 구현
- web-design-guidelines: 키보드·스크린리더·모바일·모션 접근성 점검
- error-path-analysis: 지도 미초기화, 검색 실패, 위치 거부, 결과 없음, 혜택 API 오류 점검


4. REQUIRED TOOLS — 허용 도구

- 파일 읽기·검색
- `frontend/public/index.html` 수정
- 인라인 JavaScript 문법 검사
- 로컬 브라우저 검증
- `git diff`, `git status`를 통한 소유권 확인

패키지 설치, 백엔드 수정, 커밋, push, Render 배포, 외부 계정 변경은 하지 마세요.


5. MUST DO — 필수 요구사항

5-1. 시작 기준과 충돌 방지

- 먼저 `git status --short`를 확인하세요.
- 현재 `frontend/public/index.html`에 이미 존재하는 혜택 UI 변경을 과거 버전으로 덮어쓰지 마세요.
- Codex 소유 파일인 `backend/**`를 수정하지 마세요.
- 기존 함수와 상태를 먼저 파악하고 재사용하세요.
- 다른 도구가 같은 파일을 수정 중이면 변경 내용을 읽고 증분 패치만 적용하세요.

5-2. 정보구조

- 상단 모드 전환은 다음 두 축을 유지합니다.
  - `내 주변 찾기` — 기본 선택
  - `정부 혜택 찾기`
- `내 주변 찾기` 내부에 `동네에서 뭐 할까?` 또는 비슷한 쉬운 제목의 추천 영역을 추가하세요.
- 추천 영역의 첫 번째이자 가장 강한 CTA는 `벙 맛집`입니다.
- 나머지 빠른 행동은 다음 순서로 배치하세요.
  - `점심 뭐 먹지`
  - `저녁 뭐 먹지`
  - `데이트 코스`
  - `헬스장`
  - `산책로`
  - `카페`
  - `맛집`
- 병원·약국·은행·마트 등 기존 추천 카테고리와 자유 검색창을 삭제하지 마세요.

5-3. 벙 맛집 MVP

- `벙 맛집` 버튼을 누르면 작은 조건 선택 영역을 표시하세요.
- 조건은 다음만 사용합니다.
  - 인원: 2명 / 3~4명 / 5명 이상
  - 예산: 1만원 이하 / 1~2만원 / 2만원 이상
  - 시간: 점심 / 저녁 / 야식
  - 분위기: 대화하기 좋은 / 활기찬 / 든든한 식사
- 사용자가 조건을 고르면 기존 카카오 장소검색을 재사용해 지역 맛집 후보를 보여주세요.
- 검색 결과 제목은 `모임 장소 후보`라고 표시하세요.
- 카카오 결과에 없는 단체석·예약·실제 소음·현재 영업 여부는 보장하지 마세요.
- 카드에는 `방문 전 단체 이용 가능 여부를 확인해 주세요.` 안내를 표시할 수 있습니다.
- 당근(밴드)의 화면·로고·문구·데이터·비공개 API를 복제하거나 호출하지 마세요.

5-4. 뭐 먹지 시리즈

- `점심 뭐 먹지`와 `저녁 뭐 먹지`는 검색어를 모르는 사용자를 위한 메뉴 탐색입니다.
- 사용자가 한 번 누르면 음식 성향 선택을 보여주세요.
  - 든든하게 / 가볍게 / 매콤하게 / 따뜻하게 / 새로운 메뉴
- 선택한 시간대와 성향을 기존 카카오 검색어로 변환해 주변 후보를 검색하세요.
- 임의 추천을 제공할 경우 같은 후보만 반복되지 않도록 최근 선택을 `localStorage`에 저장할 수 있습니다.
- 위치·검색기록은 서버로 전송하거나 장기간 추적하지 마세요.

5-5. 지역 데이트 코스

- 데이트 코스는 한 장소가 아니라 다음 2~3단계 후보 조합입니다.
  - 식사
  - 카페 또는 디저트
  - 산책·공원·전시·영화 중 하나
- 초기 버전은 `데이트 코스 후보`로 표시하세요.
- 실제 최적 동선, 이동시간, 운영시간, 예약 가능 여부가 검증되지 않았으면 확정 표현을 쓰지 마세요.
- 사용자가 각 단계를 다른 장소로 교체할 수 있는 구조를 우선하세요.
- 구현 범위가 커지거나 기존 파일과 충돌하면 이번 작업에서는 진입 버튼과 상태별 UI까지만 구현하고 남은 계약을 보고하세요.

5-6. 지역 헬스장

- 기존 카카오 장소검색으로 현재 위치 주변 헬스장 후보를 조회하세요.
- 현재 검증 가능한 상호, 주소, 거리, 카카오 상세 링크만 표시하세요.
- 가격·시설·혼잡도·후기·평점 정보가 없으면 `평가 좋은 헬스장`이라고 표시하지 마세요.
- 향후 지역 사용자 평가 기능이 필요하다는 안내는 개발 보고서에 기록하고 일반 화면에는 과도하게 노출하지 마세요.

5-7. 기존 카페·생활 검색 보존

- `/api/cafes`, `fetchCafes`, 반경 칩, 반경 원, 카페 마커와 목록 동기화를 유지하세요.
- 기존 카카오 키워드 검색, GPS, 지역 선택, 검색 결과 카드를 유지하세요.
- 추천 프리셋은 기존 검색 함수를 재사용하는 얇은 어댑터로 구현하세요.
- 동일 기능을 새로 복제하거나 전역 상태를 이중으로 만들지 마세요.

5-8. 정부 혜택 기능 보존

- 다음 API 계약을 유지하세요.
  - `GET /api/benefits`
  - `GET /api/data-sources`
- 404·503·502·네트워크 오류 안내를 유지하세요.
- 혜택 외부 데이터는 `textContent`와 DOM API로 안전하게 렌더링하세요.
- `신청 가능`, `100% 대상` 같은 확정 표현을 사용하지 마세요.
- 인증키 값과 환경변수 이름을 화면에 표시하지 마세요.

5-9. 상태와 접근성

- 추천 버튼은 실제 `button`을 사용하세요.
- 선택 상태는 `aria-pressed` 또는 적절한 탭·라디오 패턴으로 표현하세요.
- 조건 선택, 검색 결과 수, 로딩, 오류는 `aria-live="polite"`로 알리세요.
- 키보드만으로 모드 전환, 추천 선택, 조건 선택, 검색, 결과 선택이 가능해야 합니다.
- 360px 모바일 화면에서 가로 스크롤과 버튼 잘림이 없어야 합니다.
- 새 모션은 `prefers-reduced-motion`을 존중하세요.


6. MUST NOT DO — 금지사항

- `backend/**`, `.env*`, `.gitignore`, `render.yaml`, `package*.json`을 수정하지 마세요.
- 패키지를 설치하지 마세요.
- Git 커밋, push, 배포를 하지 마세요.
- 공공데이터 API 또는 인증키를 브라우저에서 직접 호출하지 마세요.
- 당근(밴드)의 데이터, 화면, 로고, 브랜드 자산, 비공개 API를 복제·스크래핑하지 마세요.
- 실제 사용자 평가가 없는데 평점이나 추천 순위를 만들어내지 마세요.
- 확인되지 않은 `현재 영업 중`, `예약 가능`, `단체석 있음`을 표시하지 마세요.
- 기존 카페·맛집·산책로·병원·약국·일반 검색을 혜택 기능 때문에 삭제하지 마세요.
- 다른 도구의 변경을 되돌리거나 전체 파일을 과거 버전으로 덮어쓰지 마세요.


7. CONTEXT — 저장소와 협업 계약

- 저장소: `gyeomsVibe/260705_ai-location-guide-week8-9`
- 브랜치: `main`
- 설계 기준 커밋: `949a115`
- 로컬 경로: `D:\D_Workspace_NB\-agentic-ai-workspace\-antigravity-workspace\260705_ai-location-guide-week8-9`
- Antigravity 소유: `frontend/public/index.html`
- Codex 소유: `backend/**`
- 기존 백엔드 API: `/api/cafes`
- Codex 구현 중 API: `/api/benefits`, `/api/data-sources`
- 사용자 화면의 타사 참고 개념 표기: `당근(밴드)`가 아니라 `동네 모임` 또는 `지역 친목모임`

검증 절차:

1. 변경 전후 `git status --short`를 비교하세요.
2. 인라인 JavaScript 문법 검사를 실행하세요.
3. 기존 지도·GPS·자유 검색·카페 반경 검색을 회귀검증하세요.
4. 벙 맛집·점심·저녁·데이트·헬스장·산책로 프리셋을 각각 확인하세요.
5. 혜택 성공·빈 결과·404·503·502·네트워크 실패를 확인하세요.
6. 외부 문자열에 HTML이 들어와도 실행되지 않는지 확인하세요.
7. 키보드와 360px 모바일 화면을 확인하세요.
8. `git diff --name-only`가 `frontend/public/index.html`만 포함하는지 확인하세요.

완료 보고 형식:

1. 변경 파일
2. 구현한 생활·커뮤니티 추천 기능
3. 보존한 기존 기능
4. 실행한 검증과 실제 결과
5. 현재 데이터로 확인할 수 없어 표시하지 않은 정보
6. 구현하지 못한 항목과 이유
7. Codex에게 필요한 백엔드 계약

검증 실패나 충돌을 숨기지 말고 정확한 원인과 보존 상태를 보고하세요.
```
